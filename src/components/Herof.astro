---
export interface Props {
  message: string;
}
const { message } = Astro.props;
---
<!-- Hero Section for FAQ -->
<section class="relative bg-gradient-to-r from-primary-600 to-primary-700 py-12 md:py-16 overflow-hidden">
  <div class="container mx-auto px-4">
    <div class="flex flex-col items-center text-center z-10 relative">
      <h1 class="text-4xl md:text-5xl font-bold mb-4 text-white animate-fadeIn">{message}</h1>
      <p class="text-lg mb-6 text-primary-100 max-w-2xl">Encuentra respuestas a tus dudas sobre nuestros productos, servicios, envíos y políticas de devolución</p>
      
      <div class="w-full max-w-xl bg-white rounded-lg shadow-lg p-4 mt-4">
        <div class="relative">
          <input 
            type="text" 
            placeholder="Buscar en preguntas frecuentes..." 
            class="w-full py-3 px-4 pr-12 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent"
          />
          <button class="absolute right-3 top-1/2 transform -translate-y-1/2 text-primary-600 hover:text-primary-800">
            <i class="fas fa-search text-lg"></i>
          </button>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Decorative elements -->
  <div class="absolute bottom-0 left-0 w-full h-16 bg-white" style="clip-path: polygon(0 100%, 100% 100%, 100% 0);"></div>
  
  <div class="absolute inset-0 z-0 opacity-10">
    <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
      <path fill="#ffffff" d="M0,0 L100,0 C80,20 50,40 20,60 L0,100 Z" />
      <path fill="#ffffff" d="M100,0 L100,100 L0,100 C30,80 50,60 80,20 Z" />
    </svg>
  </div>
  
  <!-- FAQ icons -->
  <div class="absolute top-10 left-10 text-primary-300 opacity-20 hidden md:block">
    <i class="fas fa-question-circle text-6xl"></i>
  </div>
  <div class="absolute bottom-10 right-10 text-primary-300 opacity-20 hidden md:block">
    <i class="fas fa-comments text-6xl"></i>
  </div>
</section>

<style>
  @keyframes fadeIn {
    0% { opacity: 0; transform: translateY(-10px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  
  .animate-fadeIn {
    animation: fadeIn 1s ease-in;
  }
</style>
